language: python
python: 3.6
sudo: false
services:
  - redis-server
install:
  - pip install tox-travis coveralls
script:
  - tox
after_success:
  - coveralls
env:
  - TOXENV=py33
  - TOXENV=py34
  - TOXENV=py35
  - TOXENV=flake8
  - TOXENV=docs
